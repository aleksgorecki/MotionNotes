<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/navigation_notes">

    <fragment
        android:id="@+id/navigation_checklists"
        android:name="com.example.motionnotes.CheckListFragment"
        android:label="Checklists"
        tools:layout="@layout/fragment_check_list" />

    <fragment
        android:id="@+id/navigation_notes"
        android:name="com.example.motionnotes.NotesFragment"
        android:label="Notes"
        tools:layout="@layout/fragment_notes" />

    <fragment
        android:id="@+id/navigation_events"
        android:name="com.example.motionnotes.EventFragment"
        android:label="Events"
        tools:layout="@layout/fragment_event" />
</navigation>